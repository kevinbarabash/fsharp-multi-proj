namespace FooLib

module Say =
    let hello name = printfn "Hello %s" name

module Arithmetic =
    let add x y = x + y
    let subtract x y = x - y
    let multiply x y = x * y
    let divide x y = x / y
